## 10. Односвязный список  

### Определение

**Односвязный список (Singly Linked List)** — это динамическая структура данных,  
состоящая из узлов (**node**), где каждый узел хранит:

1. **значение (data)**
2. **указатель на следующий элемент (next)**

> В отличие от массива, элементы списка не расположены подряд в памяти —  
> каждый элемент знает только о следующем.

### Структура узла

```cpp
struct Node {
    int value;    // значение
    Node* next;   // указатель на следующий элемент
    Node(int val) {
        value = val;
        next = nullptr;
    }
};
```
### Основные операции

| Операция         | Описание              | Асимптотика по времени | Асимптотика по памяти |
| ---------------- | --------------------- | ---------------------- | --------------------- |
| `push_front(x)`  | вставка в начало      | O(1)                   | O(1)                  |
| `push_back(x)`   | вставка в конец       | O(n)                   | O(1)                  |
| `pop_front()`    | удаление из начала    | O(1)                   | O(1)                  |
| `pop_back()`     | удаление из конца     | O(n)                   | O(1)                  |
| `insert(pos, x)` | вставка по индексу i  | O(i)                   | O(1)                  |
| `remove(pos)`    | удаление по индексу i | O(i)                   | O(1)                  |
| `find(x)`        | поиск элемента        | O(n)                   | O(1)                  |
| `empty()`        | проверка на пустоту   | O(1)                   | O(1)                  |
| `size()`         | длина списка          | O(n)                   | O(1)                  |
### Реализация

```cpp
#include <iostream>

struct Node {
    int value;    // значение
    Node* next;   // указатель на следующий элемент
    Node(int val) {
        value = val;
        next = nullptr;
    }
};

class SinglyLinkedList {
private:
    Node* head;

public:
    SinglyLinkedList() : head(nullptr) {}

    ~SinglyLinkedList() {
        while (head != nullptr) {
            Node* temp = head;
            head = head->next;
            delete temp;
        }
    }

    void push_front(int x) {
        Node* node = new Node(x);
        node->next = head;
        head = node;
    }

    void push_back(int x) {
        Node* node = new Node(x);
        if (!head) {
            head = node;
            return;
        }
        Node* cur = head;
        while (cur->next) cur = cur->next;
        cur->next = node;
    }

    void pop_front() {
        if (!head) return;
        Node* temp = head;
        head = head->next;
        delete temp;
    }

    void pop_back() {
        if (!head) return;
        if (!head->next) {
            delete head;
            head = nullptr;
            return;
        }
        Node* cur = head;
        while (cur->next->next) cur = cur->next;
        delete cur->next;
        cur->next = nullptr;
    }

    void print() {
        Node* cur = head;
        while (cur) {
            std::cout << cur->data << " ";
            cur = cur->next;
        }
        std::cout << "\n";
    }
};
```

### Особенности и отличия от массива

|Характеристика|Односвязный список|Массив|
|---|---|---|
|**Выделение памяти**|каждый узел выделяется **в куче (heap)** динамически через `new`|весь массив выделяется **целиком в памяти (stack или heap)** при создании|
|**Индексация**|❌ прямого доступа по индексу нет (нужно проходить последовательно)|✅ доступ по индексу за `O(1)`|
|**Вставка / удаление**|эффективно в начале (`O(1)` при известной ссылке)|дорого: требуется сдвиг элементов (`O(n)`)|
|**Перемещение по элементам**|только вперёд по `next`|двустороннее (через индексы)|
|**Рост структуры**|легко увеличивается — добавляем новые узлы в куче|требует перевыделения памяти (`realloc`) при переполнении|
### Использование

- Реализация **стека**, **очереди**, **хэш-таблиц** (цепочки)
- Работа с **неизвестным числом элементов**
- Когда важны **вставки и удаления**, а не быстрый доступ


## Массивы vs Связные списки

 **Массивы часто удобнее**, потому что:

- Доступ к элементу по индексу O(1), а в списке O(n)
- Меньше накладных расходов по памяти (в списке каждый узел хранит указатель)
- Кэш-память и предсказание ветвлений лучше работает с массивами

Но у **списков есть свои “подводные камни и плюсы”

---
## **Почему используют списки и где они удобны**

1. **Частые вставки/удаления в середине**
    
    - В массиве: вставка/удаление требует сдвига O(n)
        
    - В списке: имея указатель на узел → вставка/удаление O(1)
        
    - Пример: редактор текста, где буквы добавляются/удаляются по середине
        
2. **Неизвестный размер данных заранее**
    
    - В массиве нужно либо заранее знать размер, либо делать реаллокацию (амортизированно O(1))
        
    - В списке память выделяется по узлам, растёт динамически
        
3. **Кольцевые структуры / круговые очереди**
    
    - Циклический список идеально подходит для задач типа: “всегда повторяем по кругу”
        
    - Например, планировщики, ротация процессов
        
4. **Двусвязный список и быстрые удаления**
    
    - Когда есть указатель на элемент → удаляем за O(1)
        
    - В массиве нужно искать элемент → O(n)
        

---

## **Подводные камни списков, о которых спрашивают на коллоке**

- **Доступ к элементу по индексу O(n)** → нельзя быстро взять i-й элемент
    
- **Дополнительная память** на указатели → O(n) + overhead
    
- **Кэш-память хуже**, массивы хранят элементы подряд → лучше для CPU
    
- **Циклические списки** → легко попасть в бесконечный цикл при ошибке указателей
    

---

💡 **Итог:**

- Массив удобнее для **частого чтения и редких вставок**
    
- Список удобнее для **частых вставок/удалений и динамического размера**
    
- Всегда помнить **сложность операций** и **особенности реализации**
    

---