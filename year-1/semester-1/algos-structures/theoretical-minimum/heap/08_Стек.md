## 8. Стек  

### Определение

**Стек (Stack)** — это абстрактная структура данных, работающая по принципу  
**LIFO (Last In — First Out)** — _последний вошёл, первый вышел._

> Пример: стопка тарелок — снимаем сверху, добавляем сверху.

Конечно! Вот таблица с основными операциями стека, дополненная несколькими важными операциями:

### 📚 Основные операции стека

| Операция | Описание | Время | Память |
|----------|-----------|-------|---------|
| **push(x)** | добавить элемент x на вершину стека | O(1) | O(1) |
| **pop()** | удалить верхний элемент | O(1) | O(1) |
| **top() / peek()** | вернуть верхний элемент (не удаляя) | O(1) | O(1) |
| **empty()** | проверить, пуст ли стек | O(1) | O(1) |
| **size()** | вернуть количество элементов | O(1) | O(1) |

### 🔍 Дополнительные операции

| Операция | Описание | Время | Память |
|----------|-----------|-------|---------|
| **find(x)** | найти элемент x в стеке | O(n) | O(1) |
| **clear()** | очистить весь стек | O(1)* | O(1) |
| **contains(x)** | проверить наличие элемента x | O(n) | O(1) |

*Примечания:*
- **O(1)** для `clear()` предполагает реализацию через простое обнуление указателя/счетчика
- Операции **find()** и **contains()** не являются стандартными для стека и требуют последовательного просмотра элементов
- Стек работает по принципу **LIFO** (Last-In-First-Out) - последний пришел, первый ушел


### Использование стека

| Применение                   | Пример                             |
| ---------------------------- | ---------------------------------- |
| Хранение истории действий    | отмена действий в редакторах       |
| Проверка правильности скобок | `((a+b)*c)`                        |
| Обход в глубину (DFS)        | стек заменяет рекурсию             |
| Перевод выражений            | из инфиксной в постфиксную форму   |
| Вычисление выражений         | с помощью обратной польской записи |
| Обратный порядок элементов   | например, реверс строки            |

---
## **1️⃣ Стек (Stack) — LIFO**

```
push(10) → push(20) → push(30)

Верх
  30  ← top
  20
  10
Низ
```

**Операции:**

|Операция|Что делает|Сложность|
|---|---|---|
|push|добавляет элемент сверху|O(1)|
|pop|удаляет верхний элемент|O(1)|
|top|смотрит верхний элемент|O(1)|

💡 **Амортизация:** если стек на динамическом массиве → один push может быть O(n) при увеличении массива, но в среднем O(1).

---
