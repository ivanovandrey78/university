# 🔗 Бинарные отношения (Binary Relations)

## Определение
Бинарное отношение `R` между множествами `A` и `B` — это подмножество `A × B`.  
Обозначение: `a R b` ⇔ `(a,b) ∈ R`.

---

## Типы отношений
- **Однородное:** `R ⊆ A × A`  
- **Неоднородное:** `R ⊆ A × B`, где `A ≠ B`

---

## Представления отношений
- **Ориентированный граф:** вершины — элементы, дуги — связи `a → b`.  
- **Двудольный граф:** для `R ⊆ A × B`.  
- **Матрица отношений:** `⟦R⟧[i,j] = 1`, если `aᵢ R bⱼ`.

---

## Операции над отношениями
| Операция    | Формула          |                         |
| ----------- | ---------------- | ----------------------- |
| Объединение | `R ∪ S = {(a,b)  | (a,b) ∈ R ∨ (a,b) ∈ S}` |
| Пересечение | `R ∩ S = {(a,b)  | (a,b) ∈ R ∧ (a,b) ∈ S}` |
| Дополнение  | `Rᶜ = (A×B) ∖ R` |                         |
| Обратное    | `R⁻¹ = {(b,a)    | (a,b) ∈ R}`             |
| Композиция  | `R ∘ S = {(a,c)  | ∃b (aRb ∧ bSc)}`        |

---

## Свойства отношений
| Свойство | Определение |
|-----------|--------------|
| Рефлексивность | `∀a ∈ A. aRa` |
| Симметричность | `(aRb) ⇒ (bRa)` |
| Транзитивность | `(aRb ∧ bRc) ⇒ aRc` |
| Антирефлексивность | `¬(aRa)` |
| Антисимметричность | `(aRb ∧ bRa) ⇒ (a=b)` |
| Асимметричность | `(aRb) ⇒ ¬(bRa)` |
| Корефлексивность | `R ⊆ id_A` |

---

## Замыкания отношений
| Замыкание | Формула |
|------------|----------|
| Рефлексивное | `r(R) = R ∪ id_A` |
| Симметричное | `s(R) = R ∪ R⁻¹` |
| Транзитивное | `t(R) = ⋃ Rᵏ (k≥1)` |

**Алгоритм Уоршалла:** эффективный способ вычислить `t(R)` за `O(n³)`.

---

## Отношения порядка
| Тип | Свойства |
|------|-----------|
| Предпорядок | рефлексивность + транзитивность |
| Частичный порядок | рефлексивность + антисимметричность + транзитивность |
| Строгий частичный порядок | антирефлексивность + транзитивность |
| Линейный порядок | частичный порядок + сравнимость любых двух элементов |

---

## Частично упорядоченные множества (Posets)
**Poset** — пара `(A, ≤)`, где `≤` — частичный порядок.

- **Диаграмма Хассе:** отображает минимальные связи (без транзитивных рёбер).  
- **Покрытие:** `a ⋖ b`, если `a < b` и нет `c` с `a < c < b`.  
- **Цепи и антицепи:** все элементы попарно сравнимы / несравнимы.

---

## Решётки (Lattices)
Позит `(A, ≤)` — **решётка**, если любые два элемента имеют `sup` (∨) и `inf` (∧).

| Тип решётки | Условие |
|--------------|----------|
| Ограниченная | есть `⊤` и `⊥` |
| Дистрибутивная | дистрибутивные законы |
| Модулярная | если `a ≤ c`, то `a ∨ (b ∧ c) = (a ∨ b) ∧ c` |
| Дополняемая | для любого `a` существует `b`, где `a∨b=⊤` и `a∧b=⊥` |
| Булева алгебра | дистрибутивная дополняемая решётка |

---

## Вполне упорядоченные множества
Множество `(A, ≤)` — **вполне упорядочено**, если каждое непустое подмножество имеет наименьший элемент.  
**Пример:** `(ℕ, ≤)`.

**Принцип вполне упорядоченности:** любое непустое множество натуральных имеет минимальный элемент (эквивалентен индукции).

---

## Обоснованные и Нётеровы отношения
- **Обоснованное:** нет бесконечных нисходящих цепей.  
- **DCC / ACC:** условия конечности для нисходящих / восходящих цепей.  
- **Нётерово отношение:** `R⁻¹` — обоснованное.  
