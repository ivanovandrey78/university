# SAT 

---

## Boolean satisfiability (SAT)

**Идея простая:**  
Есть булева формула из переменных `x, y, z`, операций `AND (∧)`, `OR (∨)`, `NOT (¬)`.  
Нужно понять:

👉 **можно ли подставить 0/1 так, чтобы формула стала 1?**

Это **ровно** вопрос из булевой алгебры:

> существует ли набор значений, при котором выражение = 1?

**Пример**

```
(x ∨ y) ∧ (¬x ∨ z)
```

Пробуем:

- x = 0
    
- y = 1
    
- z = 1
    

Тогда:

- (0 ∨ 1) = 1
    
- (¬0 ∨ 1) = (1 ∨ 1) = 1
    

→ вся формула = 1  
➡️ **SAT-формула выполнима**

---

## Satisfiable / Unsatisfiable / Valid

### 1. Выполнимая (satisfiable)

👉 **Есть хотя бы один набор**, при котором формула = 1.

**Пример**

```
x ∨ y
```

Берём `x = 1` → всё ок.

---

### 2. Невыполнимая (unsatisfiable)

👉 **Ни при каком наборе** формула не становится 1.

С точки зрения булевой алгебры:

> выражение тождественно равно 0

**Пример**

```
x ∧ ¬x
```

Что бы ты ни подставил:

- x = 1 → 1 ∧ 0 = 0
    
- x = 0 → 0 ∧ 1 = 0
    

➡️ всегда 0 → **UNSAT**

---

### 3. Валидная / тавтология (valid)

👉 **Истинна при всех наборах**

С точки зрения булевой алгебры:

> выражение тождественно равно 1

**Пример**

```
x ∨ ¬x
```

Всегда 1 → **VALID**

---

## SAT–VALID duality

**Формула — тавтология  
⇔  
её отрицание невыполнимо**

Почему это логично:

- Если `F` **всегда = 1**,  
    то `¬F` **всегда = 0** → нет ни одного набора
    

**Пример**

```
F = x ∨ ¬x   (тавтология)
¬F = ¬x ∧ x (невыполнима)
```

👉 Это часто используют, чтобы **проверять тавтологию через SAT**.

---

## 2-SAT

**Ограничение:**  
Каждая скобка содержит **ровно два литерала**.

Пример:

```
(x ∨ y) ∧ (¬x ∨ z) ∧ (¬y ∨ ¬z)
```

Почему это важно:

- Общий SAT — сложно
    
- **2-SAT решается быстро (полиномиально)**
    

Интуиция (очень по-человечески):

Каждое `(a ∨ b)` — это:

```
если a = 0 → b обязан быть 1
если b = 0 → a обязан быть 1
```

То есть это **цепочки логических обязанностей**,  
которые можно проверить без перебора.

👉 В булевой алгебре это как система ограничений, а не поиск вслепую.

---

# Классы сложности — очень человеческое объяснение

## Главная идея (одна мысль)

> Все классы сложности — это про **сколько усилий нужно**, когда размер задачи растёт.

Не про секунды, а про:

- растёт ли время **нормально**
    
- или **взрывается**
    

---

### P — “делается сразу”

**Что это за задачи:**

> Есть алгоритм, который **всегда быстро находит ответ**.

“Быстро” = без перебора всех вариантов.

**Примеры интуитивно:**

- отсортировать числа
    
- проверить скобки
    
- найти кратчайший путь
    

👉 растёт вход — время растёт **разумно**

---

### NP — “если показать ответ, я быстро скажу, верно ли”

**Ключевая идея:**

> Решение может быть трудно найти,  
> но **проверить готовое — легко**.

Это вообще про **проверку**, а не про поиск.

SAT:

- Формула большая
    
- Перебирать все значения — долго
    
- Но если тебе **дали набор 0/1**, ты:
    
    - просто подставляешь
        
    - считаешь булево выражение
        

👉 это быстро → значит SAT ∈ NP

> NP — это задачи, где решение легко проверить.

---

## NP-hard — “не проще, чем самые трудные”

Тут вообще не важно, можно ли проверять.

Смысл:

> Любую задачу из NP можно свести к этой.

То есть:

- если умеешь решать NP-hard
    
- ты умеешь решать **всё из NP**
    

Может даже:

- не быть задачей из NP
    
- не иметь удобной проверки
    

---

## NP-complete — “король сложных задач”

Это **пересечение двух идей**:

1. задача в NP
    
2. задача не проще любой другой из NP
    

👉 **самые трудные задачи в NP**

SAT — главный пример

---

## Общие выводы из NP

> «Класс P — это задачи, которые можно эффективно решить.  
> NP — это задачи, где предложенное решение можно эффективно проверить.  
> NP-complete — самые сложные задачи внутри NP, к ним сводятся все остальные.  
> SAT — классический пример NP-complete задачи.»

---

## Теорема Кука–Левина

**Смысл без формализма:**

> Любую задачу из NP можно переписать в виде SAT

> **SAT — настолько общая задача, что любую задачу из NP можно переписать как задачу на булеву формулу.**

> **Если задача “легко проверяется”, то её можно закодировать в SAT.**

То есть:

- любая «сложная логическая задача»
    
- кодируется булевой формулой
    

👉 SAT — **универсальный язык NP**

> «Теорема Кука–Левина утверждает, что любую задачу из NP можно за полиномиальное время свести к задаче выполнимости булевой формулы. Поэтому SAT является NP-полной задачей.»

---

## Tseitin transformation

**Проблема:**  
Если просто раскрывать формулу в CNF,  
она может **взорваться по размеру**.

**Идея Цейтина:**

- вводим новые переменные
    
- каждая отвечает за кусок формулы
    
- размер растёт **линейно**
    

Важно:

- **сохраняется выполнимость**
    
- логически эквивалентной формула может не быть,  
    но SAT / UNSAT сохраняется
    

👉 В булевой алгебре это как:

> заменить сложное выражение на переменную + ограничения

---

## Resolution (резолюция)

**Правило для CNF**

Если есть:

```
(x ∨ A)
(¬x ∨ B)
```

Можно вывести:

```
(A ∨ B)
```

Интуиция:

- либо x = 1 → работает первая
    
- либо x = 0 → работает вторая
    
- значит, в любом случае должно выполняться `(A ∨ B)`
    

👉 Это **алгебраическое сокращение по переменной**

---

## Unit propagation

**Если в скобке остался один литерал — выбора нет**

Пример:

```
(x)
```

👉 x **обязан быть 1**

Почему:

- иначе формула сразу = 0
    

Это просто:

> если AND содержит один обязательный элемент, он должен быть истинным

---

## Pure literal elimination (Чистое исключение литералов)

Если переменная встречается **только в одном виде**:

- только `x`
    
- или только `¬x`
    

👉 можно сразу выбрать значение, которое делает их истинными

Пример:

```
(x ∨ y) ∧ (x ∨ z)
```

x нигде не отрицается → ставим `x = 1`

Это **безопасно**, ничего не ломает.

---

# DPLL и CDCL — мини-конспект

### DPLL (умный перебор)

- **Unit propagation**:
    
    - Если в клаузе остался один литерал → он **обязан быть 1**, иначе формула сразу = 0.
        
    - **Зачем:** сокращаем варианты, очевидное фиксируем сразу.
        
- **Pure literal elimination**:
    
    - Литерал встречается только в положительном или только в отрицательном виде → можно сразу поставить нужное значение.
        
    - **Зачем:** упрощаем формулу, не создаём конфликтов.
        
- **Выбор переменной + backtracking**:
    
    - Пробуем 0/1, если конфликт — откат назад.
        

---

### CDCL (DPLL + обучение на ошибках)

- Добавляется **анализ конфликтов**:
    
    - если возникает противоречие, алгоритм **учится**, создавая новую клаузу, чтобы больше не повторять ту же ошибку.
        
- Добавляется **backjump**:
    
    - возврат не шаг назад, а сразу к месту, где реально произошло противоречие.
        

---
- DPLL = умный перебор с упрощениями
    
- CDCL = DPLL + память об ошибках → работает намного быстрее на больших формулах
---